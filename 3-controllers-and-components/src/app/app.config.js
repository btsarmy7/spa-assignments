export function config($logProvider/*, localStorageServiceProvider*/) { //angular local storage
  'ngInject'
  $logProvider.debugEnabled(true)
  //localStorageServiceProvider.setPrefix('clickerGame')
}
